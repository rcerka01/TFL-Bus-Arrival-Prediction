package services

import models.JobDAO
import models.autogenerated.Tables.JobsRow


/**
 * Created by raitis on 10/09/15.
 */
object JobServices {


  def saveJob(job: JobsRow):Unit = {
    try {
      JobDAO.insert(job)
    }
    // <- error handler must be implemented.
  }


  def findJobsByUserId(id: Int) = {
    try {
      JobDAO.findJobsByUserId(id)
    }
    // <- error handler must be implemented.
  }


  def deleteJob(id: Int) = {
    try {
      JobDAO.deleteJob(id)
    }
    // <- error handler must be implemented.
  }

}
